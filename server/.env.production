# Production Environment Configuration
NODE_ENV=production
PORT=1314

# Database Configuration
# Your existing database settings should go here
DB_NAME=equipment_checker
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# Security Configuration
SESSION_SECRET=AudioRackLive2024SecureSessionKey!Change-This-In-Production
SITE_PASSWORD_HASH=your_site_password_hash_here

# Site Password: AudioRack2024! (change this!)
# To generate a new hash: node -e "console.log(require('crypto').createHash('sha256').update('YourNewPassword').digest('hex'))"

# Optional: Admin IP whitelist (comma-separated)
# ADMIN_IPS=192.168.1.100,203.0.113.42

# SSL/HTTPS Configuration (for reverse proxy)
# FORCE_HTTPS=true
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem

# Domain Configuration
DOMAIN=audiorack.live
ALLOWED_DOMAINS=audiorack.live,www.audiorack.live

# Rate Limiting Configuration
MAX_REQUESTS_PER_WINDOW=100
WINDOW_MS=900000
AUTH_MAX_ATTEMPTS=5
SITE_AUTH_MAX_ATTEMPTS=3